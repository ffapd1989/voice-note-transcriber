# Versões fixadas para build reproduzível (validadas em 2026-07-28, Python 3.14).
customtkinter==6.0.0
# Pillow é dependência DIRETA: o app desenha chevron, badge "+" e ícone do GitHub
# com PIL (ver _chevron_image / _plus_badge). O customtkinter 6.0 deixou de
# arrastá-lo por transitividade — sem esta linha o exe sai sem PIL e morre na
# abertura, sem console para mostrar o erro.
pillow==12.3.0
requests==2.34.2
# tkinterdnd2 fixado na versão contra a qual o app foi desenvolvido/testado.
tkinterdnd2==0.5.0
pygame-ce==2.5.7
mutagen==1.48.1
# PyOgg só publica pre-releases no PyPI; o pin exato (==) instala sem precisar de --pre.
pyogg==0.6.14a1
pyinstaller==6.21.0
